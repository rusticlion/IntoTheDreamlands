/// @description Overwrite with actor behavior
// on overworld interaction from player
// by default, provide a dialogue according to config
buildDialog(dialog_pages, dialog_choices, dialogCallback, portrait);